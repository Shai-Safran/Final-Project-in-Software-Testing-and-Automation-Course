# ====================================================================
# 🚀 Automation Exercise Web Project - Dependencies (UV Optimized)
# ====================================================================
#
# 📌 קובץ זה מותאם לעבודה עם UV - מנהל החבילות המהיר
#
# התקנה מהירה:
#   uv pip install -r requirements.txt
#
# יצירת סביבה וירטואלית + התקנה:
#   uv venv ; uv pip install -r requirements.txt
#
# הרצה ישירה בלי activation:
#   uv run python run_tests.py
#   uv run pytest tests/
# ====================================================================

# === Core Automation Dependencies ===
selenium>=4.27.0,<5.0.0
# Web automation framework - גרסה אחרונה עם Selenium Manager מובנה

# === Testing Framework ===
pytest>=8.3.0,<9.0.0
# Testing framework עיקרי

pytest-xdist>=3.6.0,<4.0.0
# הרצה מקבילית של מבחנים (uv run pytest -n auto)

pytest-order>=1.3.0,<2.0.0
# שליטה בסדר הרצת מבחנים - מתקן את האזהרות שלך

# === Reporting & HTML Output ===
pytest-html>=4.1.0,<5.0.0
# דוחות HTML מפורטים ויפים

pytest-metadata>=3.1.0,<4.0.0
# מטא-דאטה לדוחות (dependency של pytest-html)

# === Test Reliability ===
pytest-rerunfailures>=14.0,<15.0.0
# הרצה חוזרת של מבחנים שנכשלו זמנית

# === Logging and Output Enhancements ===
colorama>=0.4.6,<1.0.0
# צבעים בקונסול (תמיכה ב-Windows)

# ====================================================================
# 📝 פקודות שימושיות עם UV:
# ====================================================================
#
# יצירת venv:
#   uv venv --python 3.13
#
# הפעלת venv (Windows):
#   .venv\Scripts\activate
#
# התקנת כל התלויות (מהיר פי 10-100!):
#   uv pip install -r requirements.txt
#
# עדכון חבילה ספציפית:
#   uv pip install --upgrade selenium
#
# הוספת חבילה חדשה:
#   uv pip install faker
#   uv pip freeze > requirements.txt
#
# הצגת כל החבילות:
#   uv pip list
#
# הרצת מבחנים ישירות (בלי activation):
#   uv run python run_tests.py
#   uv run pytest tests/ -v
#   uv run pytest tests/ -n auto  # מקבילי - מהיר!
#
# סנכרון מדויק לפי הקובץ:
#   uv pip sync requirements.txt
#
# ניקוי cache (לעיתים נדירות):
#   uv cache clean
#
# ====================================================================
# 🎯 תלויות אופציונליות (להוסיף בעתיד):
# ====================================================================
#
# allure-pytest==2.13.5        # דוחות Allure מתקדמים
# pytest-timeout==2.3.1        # timeout אוטומטי למבחנים
# pytest-cov==5.0.0           # כיסוי קוד
# faker==28.4.1               # יצירת דאטה אקראי
# black==24.8.0               # code formatter
# ruff==0.6.9                 # linter מהיר
# ====================================================================