
# 🌐 Automation Exercise Web Project - Selenium & Pytest Framework

פרויקט זה מכיל אוטומציה של בדיקות אינטרנט (UI) עבור אתר **Automation Exercise**, שנבנה באמצעות **Python**, ספריית **Selenium WebDriver** ומסגרת הבדיקות **Pytest**. המטרה היא לוודא את תקינות הפונקציונליות הקריטית באתר, תוך שימוש בארכיטקטורה מופרדת (Framework) המבטיחה קריאות, שימוש חוזר בקוד וקלות תחזוקה.

---

## 🛠️ טכנולוגיות

* **שפת תכנות:** Python 3.x
* **Framework:** Pytest
* **אוטומציה:** Selenium WebDriver
* **ניהול דפדפן:** `webdriver-manager`
* **לוגינג:** `colorama`

---

## 📂 מבנה הפרויקט

הפרויקט מאורגן באופן היררכי עם הפרדה בין לוגיקת הליבה ללוגיקת הבדיקות:

```

automation exercise\_web\_project/
├── .venv/                      \# (סביבה וירטואלית ותלויות)
├── conftest.py                 \# (Fixture גלובלי - הגדרת Driver)
├── framework/                  \# (תיקיית Core - מכילה לוגיקה חוזרת)
│   ├── **init**.py
│   ├── logger.py               \# (פונקציות לוגים)
│   └── actions.py              \# (כל פונקציות ה-Selenium וה-Helpers)
└── tests/                      \# (תיקיית הבדיקות)
├── **init**.py
├── test\_products\_selecting\_and\_adding\_to\_cart.py
├── test\_login-signup\_page.py
├── test\_navigation\_to\_test\_cases.py
└──run_tests.py                 \# (הרצת הבדיקות)

````

## ⚙️ ארכיטקטורה ופונקציות ליבה (`framework/`)

### 1. תצורת Driver גלובלית (`conftest.py`)
קובץ זה מגדיר את ה-Fixture הגלובלי **`driver`**. ה-Fixture אחראי:
* לאתחול (Setup) מופע Chrome WebDriver באמצעות `webdriver-manager`.
* [cite_start]הגדרת ה-Driver לרוץ במצב **`headless=new`** עם רזולוציה של `1920x1080`[cite: 5].
* הבטחת סגירת הדפדפן (Teardown) בסוף הריצה.

### 2. פונקציות ליבה (`framework/actions.py`)
קובץ זה מכיל את כל פעולות ה-Selenium המבוססות על חוסן:

| פונקציה | תיאור |
| :--- | :--- |
| `safe_click(driver, element)` | [cite_start]מבצע לחיצה בטוחה, כולל גלילה, המתנה ללחיצות, וטיפול בשגיאות נפוצות כגון `StaleElementReferenceException`[cite: 7, 8]. |
| `wait_for_clickable(driver, by_type, locator)` | [cite_start]ממתין עד שאלמנט יהיה לחיץ ומחזיר אותו[cite: 8]. |
| `retry_on_stale(func, *args)` | [cite_start]Utility המפעיל פונקציה שוב במקרה של `StaleElementReferenceException`[cite: 14, 15]. |
| `remove_all_overlays(driver)` | [cite_start]מנסה להסיר מודאלים או overlays המפריעים ללחיצה באמצעות לחיצה או JavaScript[cite: 10, 11]. |
| `safe_find(driver, by, value)` | [cite_start]מחפש אלמנט בבטחה ומחזיר `None` אם לא נמצא, כדי למנוע קריסת בדיקות[cite: 12, 13]. |
| `logout_if_logged_in(driver)` | [cite_start]בודק את קיומו של קישור "Logout" ומבצע התנתקות במידת הצורך[cite: 15, 16]. |

---

## 🔬 פירוט מקרי הבדיקה (`tests/`)

### 1. `test_login-signup_page.py`
בדיקות המתמקדות בניהול חשבון:
* [cite_start]**test_register_user**: רישום מלא של משתמש חדש עם פרטים רנדומליים[cite: 37].
* [cite_start]**test_login_wrong_user**: אימות כי נכשלת כניסה עם פרטים שגויים, וכי הודעת השגיאה המתאימה מוצגת[cite: 39, 40].

### 2. `test_products_selecting_and_adding_to_cart.py`
בדיקות המתמקדות בזרימת הקנייה:
* [cite_start]**test_navigate_to_products**: ניווט מוצלח לעמוד המוצרים[cite: 51, 52].
* [cite_start]**test_click_women_category**: הרחבת קטגוריית Women[cite: 52, 53].
* [cite_start]**test_view_blue_top_product**: צפייה במוצר ספציפי לאחר ריחוף (Hover)[cite: 54].
* [cite_start]**test_add_to_cart...**: הוספת מוצרים לעגלה ואימות מוצלח של הפריטים והמחירים בעגלה[cite: 55, 56, 57, 58, 59].

### 3. `test_navigation_to_test_cases.py` & `test_testcases_page.py`
בדיקות הניווט לעמוד "Test Cases":
* [cite_start]**test_navigate_to_test_cases**: מנווט לעמוד, מאמת את ה-URL וסופר את מקרי הבדיקה[cite: 43, 44, 45]. [cite_start]מבצע לחיצה על ראשי התיבות (אקורדיון) כדי לוודא את הצגת התוכן[cite: 46, 47, 48, 49].
* [cite_start]**test_testcases_page**: בדיקה פשוטה ומיותרת (כפולה) של ניווט וספירת מקרי בדיקה[cite: 60, 61].

---

## 🚀 הגדרה והפעלה

### 1. התקנת תלויות
```bash
# הפעלת הסביבה הווירטואלית
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/macOS

# התקנת התלויות (אם קיימות ב-requirements.txt)
pip install -r requirements.txt
````

### 2\. הרצת הבדיקות וייצוא דוחות HTML דינמיים
run_tests.py

             הגדרות קונפיגורציה (Configuration Settings)
# ==============================================================================

# 💡 1. קביעת מצב Headless:
#    True: הדפדפן ירוץ ברקע (מומלץ ל-CI).
#    False: הדפדפן יופיע על המסך (מומלץ לניפוי שגיאות).
RUN_HEADLESS_MODE = False  # 💡 שינוי: ברירת מחדל לגלוי

# 💡 2. קביעת מצב הרצה:
#    True: מריץ במקביל באמצעות pytest-xdist (-n auto).
#    False: מריץ בטור (Sequential) (מומלץ לפרויקטים תלויי-סדר).
RUN_PARALLEL_MODE = False  # 💡 ברירת מחדל: טור
x/macOS/Git Bash** | `REPORT_NAME=$(date +%Y%m%d_%H%M%S)` <br> `pytest -v --html="reports/report_$REPORT_NAME.html" --self-contained-html` |
